# Local environment files
.env
realstate/.env

## (Legacy) realstate paths (project renamed to real_estate); keep defensive ignores for old path & new path data remnants
realstate/properties/**
realstate/logs/**
projects/real_estate/properties/data/**
projects/real_estate/properties/images/**
projects/real_estate/logs/**

# Common image types
*.jpg
*.jpeg
*.png

# Logs & artifacts (new monorepo layout)
artifacts/**
!artifacts/.gitkeep
!artifacts/README.md

# Legacy paths (pre-refactor) kept for safety; can remove later (scheduled for cleanup)
emails_out/**
.env
.venv
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.cache/

# Envs / deps
.venv/
.venv*/
.env
.env.*

# Outputs
collector_tmp/
*.zip
dist/
build/

# VS Code (track only workspace configs)
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Local workspace artifacts under certificaciones/
certificaciones/.vscode/

# Local datasets / extracted artifacts (do not commit)
certificaciones/snowflakeIA/TestAssesments/data/

# Vendor/course materials (do not commit)
certificaciones/snowflakeIA/generativeai-with-snowflake/

# Local utility scripts (keep untracked unless explicitly needed)
certificaciones/snowflakeIA/ocr_extract.py

# Media (ignore JPEGs/JPGs)
*.jpg
*.JPG
*.jpeg
*.JPEG

# Easybrokers images (explicit path)
easybrokers/properties/images/

## Telus compare local artifacts now under artifacts/; keep legacy ignores defensively
telus_compara_imagenes/.user_data/**
telus_compara_audios/.user_data/**
telus_compara_audios/emails_out/**
telus_compara_audios/logs/**

## Explicit sensitive auth files (even if moved later)
realstate/properties/.auth_wiggot.json
realstate/properties/.auth_*.json
